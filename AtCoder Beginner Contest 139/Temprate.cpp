#include <bits/stdc++.h>
using namespace std;
 
int main() {
  int a;
  // 変数aで入力を受け取る
  cin >> a;
  cout << a  << endl;

//配列
vector<int> vec(N);
for (int i = 0; i < N; i++) {
cin >> vec.at(i);
}

//配列の要素数
配列変数.size();

//初期値の指定
vector<型> 配列名(要素数, 初期値);

//要素の追加
vec.push_back(10);


}